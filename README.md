# SimpleSpawn

Small Minecraft plugin (Spigot) that provides `/setspawn` and `/spawn` with cooldowns and a configurable join welcome message.

Build:

```powershell
mvn clean package
```

Drop the JAR from `target/` into your server `plugins/` folder.
